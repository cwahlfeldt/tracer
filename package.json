{
    "name": "tracer",
    "version": "0.0.1-alpha",
    "type": "module",
    "description": "Tracer game",
    "scripts": {
        "start": "vite",
        "build": "vite build",
        "test": "vitest run",
        "test-watch": "vitest",
        "test-ui": "vitest --ui",
        "deploy": "rm -rf ./dist && npm run build && gh-pages -d ./dist && rm -rf ./dist"
    },
    "author": "cwahlfeldt",
    "license": "ISC",
    "devDependencies": {
        "@vitest/ui": "^0.24.5",
        "gh-pages": "^4.0.0",
        "prettier": "2.7.1",
        "reselect": "^4.1.6",
        "typescript": "^4.8.4",
        "vite": "^3.2.2",
        "vitest": "^0.24.5"
    },
    "dependencies": {
        "fast-memoize": "^2.5.2",
        "ts-essentials": "^9.3.0"
    }
}
